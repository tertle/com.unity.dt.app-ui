using NUnit.Framework;
using Unity.AppUI.Redux;

namespace Unity.AppUI.Tests.Redux
{
    // [TestFixture]
    // [TestOf(typeof(Store<>.CreationContext))]
    // class CreationContextTests
    // {
    //     [Test]
    //     public void CanCreate_CreationContext()
    //     {
    //         var store = new Store((state, _) => state, new PartitionedState(), null);
    //         var context = new Store.CreationContext(store);
    //         Assert.IsNotNull(context);
    //         Assert.IsNotNull(context.reducer);
    //         Assert.IsNotNull(context.state);
    //         Assert.IsNotNull(context.dispatcher);
    //         Assert.AreEqual(store, context.store);
    //     }
    // }
}
